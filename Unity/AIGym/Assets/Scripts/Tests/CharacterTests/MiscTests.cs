/*
This program has been developed by students from the bachelor Computer Science
at Utrecht University within the Software and Game project course.

©Copyright Utrecht University (Department of Information and Computing Sciences)
*/

using System.Collections;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.SceneManagement;
using UnityEngine.TestTools;

using NotImplementedException = System.NotImplementedException;

namespace CharacterTests
{
    public class MiscTests
    {
        /* 
        float MARGIN_OF_ERROR = 0.0005f;

        GameObject character;
        GameObject camera;
        Vector3 initCharacter, initCamera, initCharacterAngle;
        Vector3 finalCharacter, finalCamera, finalCharacterAngle;

        [SetUp]
        public void AlwaysRunBefore()
        {
            SceneManager.LoadScene("Scenes/Main");
        }
        [UnityTest]
        public IEnumerator Does_Character_Take_Screenshot()
        {
            yield return null;

            SetupTest();

            byte[] testImage = character.GetComponent<Character>().TakeScreenshot();

            yield return null;

            FinalValues();

            Assert.IsNotNull(testImage);
        }
        private void SetupTest()
        {
            character = GameObject.FindWithTag("Player");
            camera = GameObject.FindWithTag("Agent1");

            initCharacter = character.transform.position;
            initCamera = camera.transform.eulerAngles;
            initCharacterAngle = character.transform.eulerAngles;
        }

        private void FinalValues()
        {
            finalCharacter = character.transform.position;
            finalCamera = camera.transform.eulerAngles;
            finalCharacterAngle = character.transform.eulerAngles;
        }
 */

    }
}
